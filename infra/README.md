# Infrastructure

This directory will contain reviewed Google Cloud configuration. Initial limits
are Cloud Run region `asia-south1`, minimum instances zero, maximum instances
one, and no GPU or Kubernetes resources.
